# Telegram Userbot для рассылки (tg_spam_bot)

Простой и эффективный скрипт на Python для автоматизации рассылки сообщений в Telegram по списку чатов. Работает на базе библиотеки **Telethon**.

##  Функционал
* **Userbot**: Работает от имени вашего личного аккаунта.
* **Рандомизация**: Используются случайные задержки между сообщениями и циклами рассылки, чтобы минимизировать риск блокировки.
* **Безопасность**: Настроен на использование переменных окружения для защиты API данных.

## 🛠 Технологии
* Python 3.10+
* Telethon — библиотека для работы с Telegram API.
* Asyncio — для асинхронной работы скрипта.

##  Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/Nikita-tg-python/tg_spam_bot.git](https://github.com/Nikita-tg-python/tg_spam_bot.git)
    cd tg_spam_bot
    ```

2.  **Настройте виртуальное окружение:**
    ```bash
    python -m venv .venv
    source .venv/bin/activate  # Для Linux/macOS
    # .venv\Scripts\activate  # Для Windows
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

##  Настройка

1.  Создайте файл `.env` в корневой папке проекта.
2.  Добавьте в него ваши данные от Telegram (получить их можно на [my.telegram.org](https://my.telegram.org)):
    ```text
    API_ID=ваш_api_id
    API_HASH=ваш_api_hash
    ```
3.  Отредактируйте список `target_chats` в файле `spam_bot.py`, добавив ссылки на нужные чаты или их ID.

## 🚦 Запуск
```bash
python spam_bot.py
